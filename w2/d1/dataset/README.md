This folder contains the current working dataset for the W2-D1 submission:

- `alerts_sample.jsonl`
- `services.json`

`alerts_sample.jsonl` was copied from the local file provided by the user.
`services.json` was created from the GeekShop topology JSON provided in the chat.
